node_modules/
*.pdf

# Stray research fetches from background hunt agents (legislation dumps, scraped pages).
# Working artifacts belong in the session scratchpad, not the repo.
clt.html
*.html.gz

# Stray scratch scripts dropped in the repo ROOT by background agents.
# Real tooling lives in bin/, pipeline/ or research/ - never at the root.
/*.py

__pycache__/
node_modules/
package-lock.json
package.json
ACOLHE_Business_Overview.pdf
